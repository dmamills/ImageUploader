extends layout

block content
	div.container.span10.offset2
		.hero-unit
			h1 Uploaded Image
			a(href='/uploads/#{imgname}', class='thumbnail')
				img(src='/uploads/#{imgname}', width='260px',height='180px')
			br
			a(href='/uploads/#{imgname}') Direct link

extends layout

block content
	div.container
		.hero-unit
			h1 Uploaded Image
			a(href='/uploads/#{img.urlName}', class='thumbnail')
				img(src='data:#{img.img.contentType};base64,#{img.img.stringified}', width='260px',height='180px')
			br
			a(href='/uploads/#{img.urlName}') Direct link
			br
			p Tags:
			span(id='tags') #{img.tags}
			script(type='text/javascript')
				var tags = $('#tags').html();
				tags = tags.split(',');
				for(var i =0; i < tags.length;i++) {
					tags[i] = '<a href="/recent/'+tags[i] +'"> '+tags[i]+'</a>' 
				}
				$('#tags').html(tags);